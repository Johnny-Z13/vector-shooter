{
  "name": "vector-shooter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "docs:balance": "node scripts/update-balance-docs.mjs",
    "hooks:install": "git config core.hooksPath .githooks",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
